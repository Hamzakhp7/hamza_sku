# 📖 USAGE GUIDE - دليل الاستخدام الشامل

## 🎯 دليل المبتدئين (Beginner's Guide)

### الخطوة 1: التثبيت

```bash
# 1. قم بتنزيل المشروع
git clone <repository-url>
cd pentest_c2_real

# 2. شغّل سكريبت التثبيت
chmod +x setup.sh
./setup.sh

# 3. انتظر حتى يكتمل التثبيت
```

### الخطوة 2: بدء Dashboard

```bash
# شغّل Dashboard:
python3 pentest_dashboard.py

# يجب أن ترى:
# ╔═══════════════════════════════════════════════════════════╗
# ║   🔥 PenTest C2 Dashboard - REAL VERSION                 ║
# ╚═══════════════════════════════════════════════════════════╝
# 📍 Dashboard URL: http://localhost:5000
# 🔐 Password: hamza_sku_2026
```

### الخطوة 3: تسجيل الدخول

1. افتح متصفحك (Chrome/Firefox/Edge)
2. اذهب إلى: `http://localhost:5000`
3. ستظهر صفحة Login جميلة
4. أدخل كلمة المرور: `hamza_sku_2026`
5. اضغط "ACCESS DASHBOARD"

### الخطوة 4: بدء Listener

1. في قسم "🎛️ Listener Control":
   - **Host**: اترك `0.0.0.0` (للاستماع على جميع الواجهات)
   - **Port**: أدخل `4444` (أو أي منفذ تريده)
2. اضغط زر "🚀 START SERVER"
3. يجب أن يتغير Status إلى "ONLINE" 🟢

### الخطوة 5: الاتصال من الهدف

#### من جهاز Linux/Mac:
```bash
# معرفة IP الخاص بجهاز Dashboard:
# انظر في قسم "Your IP addresses" في أداة التشخيص
# أو:
ip addr show | grep "inet " | grep -v "127.0.0.1"

# ثم من الجهاز الهدف:
nc 192.168.1.100 4444
# استبدل 192.168.1.100 بـ IP جهازك
```

#### من Android (Termux):
```bash
# ثبّت netcat أولاً:
pkg install netcat

# اتصل:
nc YOUR_COMPUTER_IP 4444
```

#### من Windows:
```bash
# استخدم ncat (من Nmap) أو netcat:
nc.exe YOUR_COMPUTER_IP 4444
```

### الخطوة 6: مشاهدة الهدف في Dashboard

- بعد الاتصال بـ 2-5 ثواني:
  - ستظهر alert: "🎯 New Victim Detected!"
  - سيظهر الهدف في جدول "Active Targets"
- إذا لم يظهر:
  - اضغط زر "🔍 SCAN"
  - انتظر ثانيتين
  - يجب أن يظهر الآن

---

## 🎮 الميزات المتقدمة (Advanced Features)

### 1. تنفيذ الأوامر (Command Execution)

#### في قسم Terminal:
```bash
# أمثلة على الأوامر:

# معلومات النظام:
uname -a
cat /etc/os-release

# معلومات الشبكة:
ip addr show
ifconfig
netstat -tuln

# معلومات العمليات:
ps aux
top -bn1

# معلومات القرص:
df -h
du -sh /home

# اختبار الاتصال:
ping -c 3 google.com
```

#### ملاحظات:
- الأوامر تُنفذ على **جهاز Dashboard** (ليس على الهدف)
- للتنفيذ على الهدف، تحتاج reverse shell حقيقي
- اضغط Enter أو زر "▶ Execute" للتنفيذ

### 2. المراقبة المستمرة (Live Monitoring)

#### الإحصائيات المباشرة:
- **Commands**: عدد الأوامر المنفذة
- **Sessions**: عدد الأهداف النشطة
- **CPU**: استخدام المعالج الحالي
- **Memory**: استخدام الذاكرة الحالية
- **Uptime**: وقت تشغيل Dashboard

#### تحديث تلقائي:
- الإحصائيات تتحدث كل 2 ثانية
- قائمة Targets كل 3 ثواني
- Logs فورية عبر WebSocket

### 3. الفحص اليدوي (Manual Scanning)

#### متى تستخدم SCAN:
- عند الشك في وجود اتصال لم يُكتشف
- بعد اتصال جديد من هدف
- عند مشاكل في الكشف التلقائي
- للتحقق من جميع الاتصالات النشطة

#### كيفية الاستخدام:
1. اذهب لقسم "🎯 Active Targets"
2. اضغط زر "🔍 SCAN"
3. انتظر 1-2 ثانية
4. ستظهر جميع الاتصالات النشطة

### 4. قراءة السجلات (Log Analysis)

#### أنواع السجلات:
- **SUCCESS** (أخضر): عمليات ناجحة
- **ERROR** (أحمر): أخطاء
- **WARNING** (برتقالي): تحذيرات
- **INFO** (أزرق): معلومات

#### أمثلة على السجلات المهمة:
```
✅ Admin logged in successfully
🚀 Listener started on 0.0.0.0:4444
🎯 New target detected: 192.168.1.50
💻 Executing: uname -a
⏹️ Listener stopped
```

---

## 🔧 السيناريوهات العملية (Practical Scenarios)

### سيناريو 1: اختبار الشبكة المحلية

```bash
# الهدف: فحص جميع الأجهزة في الشبكة

# 1. شغّل Dashboard
python3 pentest_dashboard.py

# 2. ابدأ Listener على منفذ 4444

# 3. من كل جهاز في الشبكة:
nc YOUR_IP 4444

# 4. راقب Dashboard - يجب أن تظهر جميع الأجهزة

# 5. استخدم SCAN للتأكد من اكتشاف الجميع
```

### سيناريو 2: اختبار أمان Firewall

```bash
# الهدف: اختبار قواعد Firewall

# 1. فعّل Firewall:
sudo ufw enable

# 2. حاول الاتصال من جهاز آخر:
nc YOUR_IP 4444
# يجب أن يفشل

# 3. اسمح بالمنفذ:
sudo ufw allow 4444

# 4. حاول مرة أخرى:
nc YOUR_IP 4444
# يجب أن ينجح الآن

# 5. راقب في Dashboard
```

### سيناريو 3: اختبار IDS/IPS

```bash
# الهدف: اختبار نظام كشف التسلل

# 1. شغّل IDS (مثل Snort) على الشبكة

# 2. ابدأ Listener في Dashboard

# 3. حاول الاتصال من أجهزة متعددة

# 4. راقب:
#    - Dashboard (هل اكتُشفت الاتصالات؟)
#    - IDS (هل سجّل التنبيهات؟)

# 5. حلل النتائج
```

### سيناريو 4: التدريب والتعليم

```bash
# الهدف: تعليم مفاهيم C2

# 1. اعرض Dashboard على projector

# 2. اشرح المكونات:
#    - Listener (ما هو؟ كيف يعمل؟)
#    - Targets (كيف يتم الكشف؟)
#    - Commands (كيف تُنفذ؟)

# 3. نفّذ اتصال حي:
nc YOUR_IP 4444

# 4. اشرح كيف ظهر في Dashboard

# 5. نفّذ أوامر ووضّح النتائج
```

---

## 🎯 نصائح للاستخدام الفعّال (Pro Tips)

### 1. اختيار المنفذ المناسب

```bash
# منافذ موصى بها:
4444  # المنفذ الافتراضي (Metasploit)
8080  # بديل شائع
9999  # آخر شائع
31337 # "elite" في لغة الهاكرز

# منافذ يجب تجنبها:
22    # SSH - قد يسبب ارتباك
80    # HTTP - يحتاج root
443   # HTTPS - يحتاج root
3306  # MySQL - قد يكون مشغول
```

### 2. استخدام Host الصحيح

```bash
# 0.0.0.0 - للاستماع على جميع الواجهات (موصى به)
Host: 0.0.0.0

# 127.0.0.1 - للاختبار المحلي فقط
Host: 127.0.0.1

# IP محدد - للاستماع على واجهة واحدة
Host: 192.168.1.100
```

### 3. مراقبة الأداء

```bash
# راقب استخدام الموارد:
# في Dashboard → System Statistics

# إذا كان CPU عالي (>50%):
# - قلل معدل التحديث
# - أوقف عمليات غير ضرورية

# إذا كانت Memory عالية (>80%):
# - أعد تشغيل Dashboard
# - امسح السجلات القديمة
```

### 4. إدارة السجلات

```bash
# السجلات تتراكم بسرعة
# Dashboard يحتفظ بآخر 100 سجل فقط

# لحفظ السجلات:
# انسخها من قسم "System Logs"

# أو استخدم التصدير (إذا متوفر)
```

### 5. أمان الاستخدام

```bash
# دائماً:
✅ استخدم في شبكة معزولة
✅ احصل على إذن كتابي
✅ غيّر كلمة المرور الافتراضية (للإنتاج)
✅ فعّل Firewall بعد الاختبار
✅ امسح السجلات بعد الانتهاء

# أبداً:
❌ تستخدم على شبكة عامة
❌ تختبر بدون إذن
❌ تترك Dashboard مفتوح على الإنترنت
❌ تشارك كلمة المرور
❌ تحتفظ بمعلومات حساسة
```

---

## 🔍 استكشاف الأخطاء السريع (Quick Troubleshooting)

### المشكلة: Dashboard لا يبدأ
```bash
# الحل:
pkill -f pentest_dashboard.py
pip3 install -r requirements.txt --break-system-packages
python3 pentest_dashboard.py
```

### المشكلة: لا تظهر Targets
```bash
# الحل:
# 1. تأكد من Status = ONLINE
# 2. اضغط زر SCAN
# 3. تحقق من الاتصال:
netstat -tn | grep :4444
```

### المشكلة: Port مشغول
```bash
# الحل:
sudo lsof -ti:4444 | xargs kill -9
# ثم ابدأ مرة أخرى
```

### المشكلة: بطء شديد
```bash
# الحل:
# أغلق برامج غير ضرورية
# قلل معدل التحديث في الكود
# استخدم متصفح أخف
```

---

## 📊 قراءة معلومات Target

### معلومات معروضة في الجدول:

```
| Status | OS | Device | IP Address | Last Seen |
```

#### Status:
- 🟢 ONLINE: متصل حالياً
- ⚪ OFFLINE: انقطع الاتصال

#### OS:
- Linux/Android: TTL = 64
- Windows: TTL = 128
- iOS/MacOS: TTL = 255 أو 64
- Unknown: لم يمكن التحديد

#### Device:
- Unknown Device: إذا لم يُحدد
- أو اسم الجهاز إذا تم اكتشافه

#### IP Address:
- عنوان IP الحقيقي للجهاز المتصل

#### Last Seen:
- آخر مرة شوهد فيها الجهاز نشط
- بصيغة: YYYY-MM-DD HH:MM:SS

---

## 🎓 التعلم والممارسة (Learning & Practice)

### للمبتدئين:
1. ابدأ بالاتصال من نفس الجهاز (localhost)
2. ثم جرب من جهاز آخر في الشبكة
3. اقرأ جميع السجلات لفهم ما يحدث
4. جرب أوامر بسيطة في Terminal
5. اقرأ الوثائق بالكامل

### للمتقدمين:
1. جرب منافذ مختلفة
2. اختبر مع Firewall مفعّل
3. حاول الاتصال من شبكات مختلفة
4. راقب الحركة بـ Wireshark
5. عدّل الكود لإضافة ميزات جديدة

### مشاريع ممارسة:
1. أنشئ شبكة اختبار محلية
2. اختبر أمان أجهزتك الخاصة
3. تدرب على سيناريوهات Red Team
4. تعلم كيفية كشف مثل هذه الأدوات (Blue Team)

---

## ✅ Checklist يومي للاستخدام

```
قبل البدء:
□ قرأت التحذيرات الأمنية
□ حصلت على إذن (إن لزم)
□ شغّلت Dashboard
□ سجّلت دخول
□ بدأت Listener

أثناء الاستخدام:
□ راقب السجلات باستمرار
□ تحقق من الإحصائيات
□ استخدم SCAN عند الحاجة
□ وثّق النتائج

بعد الانتهاء:
□ أوقف Listener
□ امسح السجلات الحساسة
□ سجّل خروج
□ أوقف Dashboard
□ فعّل Firewall
```

---

**📝 ملاحظة نهائية:**

هذا الدليل يغطي الاستخدام الأساسي والمتقدم. للحصول على مساعدة إضافية:
- اقرأ README.md
- اقرأ TROUBLESHOOTING.md
- راجع تعليقات الكود
- استخدم diagnose.sh

**Made with 🔥 by HAMZA SKU**
**Version: 1.0.0 - Real Edition**
