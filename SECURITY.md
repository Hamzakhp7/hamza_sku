# 🔐 دليل الأمان والحماية

## معلومات تسجيل الدخول

### 🔑 بيانات الوصول الرئيسية

**كلمة المرور الرئيسية:** `hamza_sku_2026`

**⚠️ مهم جداً:**
- هذه كلمة المرور الوحيدة للوصول إلى Dashboard
- يجب حفظها في مكان آمن
- لا تشاركها مع أي شخص غير مصرح له

---

## 🛡️ نظام الأمان المتقدم

### 1. آلية المصادقة

#### التشفير
```python
# كلمة المرور مشفرة باستخدام SHA-256
Password: hamza_sku_2026
SHA-256 Hash: [auto-generated]
```

#### طريقة العمل
1. المستخدم يدخل كلمة المرور في صفحة Login
2. النظام يقوم بتشفيرها SHA-256
3. مقارنة الـ Hash مع القيمة المخزنة
4. إذا تطابقت → تسجيل دخول ناجح
5. إذا لم تتطابق → رفض الوصول

### 2. نظام الجلسات (Sessions)

```python
Session Duration: 24 ساعة
Session Type: Permanent
Auto-Logout: عند إغلاق المتصفح (اختياري)
```

**الميزات:**
- ✅ جلسة آمنة مشفرة
- ✅ انتهاء صلاحية تلقائي
- ✅ حماية من CSRF
- ✅ Secret Key عشوائي

### 3. حماية API Endpoints

جميع الـ endpoints محمية بـ `@login_required` decorator:

```
✅ /api/status - محمي
✅ /api/start - محمي
✅ /api/stop - محمي
✅ /api/command - محمي
✅ /api/logs - محمي
✅ /api/history - محمي
❌ /login - متاح للجميع (للدخول فقط)
```

---

## 🔒 أفضل الممارسات الأمنية

### للاستخدام اليومي

#### ✅ افعل:
1. **غيّر كلمة المرور** في بيئة الإنتاج
2. **استخدم HTTPS** بدلاً من HTTP
3. **قيّد الوصول بالـ IP** للشبكة المحلية فقط
4. **سجّل الخروج** بعد الانتهاء
5. **راجع Logs** بانتظام
6. **حدّث التطبيق** بانتظام
7. **استخدم Firewall** لحماية المنفذ

#### ❌ لا تفعل:
1. **لا تشارك** كلمة المرور
2. **لا تعرض** Dashboard على الإنترنت مباشرة
3. **لا تستخدم** في شبكات عامة
4. **لا تترك** الجلسة مفتوحة
5. **لا تحفظ** كلمة المرور في المتصفح

---

## 🔧 تغيير كلمة المرور

### الطريقة 1: تعديل الكود مباشرة

```python
# في ملف pentest_dashboard.py
MASTER_PASSWORD = "YOUR_NEW_STRONG_PASSWORD_HERE"
```

### الطريقة 2: استخدام متغيرات البيئة (موصى به)

```bash
# في terminal
export PENTEST_PASSWORD="your_new_password"

# ثم عدّل الكود:
MASTER_PASSWORD = os.getenv('PENTEST_PASSWORD', 'hamza_sku_2026')
```

### الطريقة 3: ملف config منفصل

```python
# في config.py
MASTER_PASSWORD = "your_new_password"

# في pentest_dashboard.py
from config import MASTER_PASSWORD
```

---

## 🚨 التعامل مع محاولات الاختراق

### مراقبة محاولات الدخول الفاشلة

النظام يسجل تلقائياً:
```
[WARNING] Failed login attempt from 192.168.1.100
[WARNING] Failed login attempt from 192.168.1.100
[WARNING] Failed login attempt from 192.168.1.100
```

### إجراءات عند الاشتباه

1. **راجع Logs فوراً**
2. **غيّر كلمة المرور**
3. **حدّد IPs المسموح لها**
4. **فعّل Firewall**
5. **أوقف الخادم إذا لزم الأمر**

---

## 🔐 ميزات أمان إضافية (للمطورين)

### 1. إضافة Rate Limiting

```python
from flask_limiter import Limiter

limiter = Limiter(app, default_limits=["5 per minute"])

@app.route('/login', methods=['POST'])
@limiter.limit("3 per minute")
def login():
    # ...
```

### 2. إضافة Two-Factor Authentication (2FA)

```python
# استخدم مكتبة pyotp
import pyotp

totp = pyotp.TOTP('base32secret')
totp.verify(user_input_code)
```

### 3. إضافة IP Whitelist

```python
ALLOWED_IPS = ['192.168.1.0/24', '127.0.0.1']

@app.before_request
def check_ip():
    if request.remote_addr not in ALLOWED_IPS:
        abort(403)
```

### 4. تفعيل HTTPS

```bash
# استخدم SSL certificate
openssl req -x509 -newkey rsa:4096 -nodes -out cert.pem -keyout key.pem -days 365

# في الكود:
socketio.run(app, ssl_context=('cert.pem', 'key.pem'))
```

---

## 📊 سجلات الأمان

### أنواع السجلات المسجلة

```
✅ Login successful from 192.168.1.50
❌ Failed login attempt from 192.168.1.100
ℹ️ User logged out
ℹ️ Dashboard initialized
⚡ Listener started on port 4444
⚠️ Listener stopped
```

### أين تُحفظ السجلات؟

```
1. في الذاكرة (مؤقت)
2. في واجهة Dashboard (System Logs)
3. يمكن إضافة حفظ في ملف:
   logs/security.log
   logs/access.log
```

---

## 🔍 فحص الثغرات

### نقاط يجب فحصها

- [ ] Session hijacking
- [ ] SQL Injection (غير موجود - لا نستخدم DB)
- [ ] XSS attacks
- [ ] CSRF attacks
- [ ] Brute force attacks
- [ ] Command injection
- [ ] Path traversal

### أدوات الفحص الموصى بها

```bash
# Nmap
nmap -p 5000 localhost

# Nikto
nikto -h http://localhost:5000

# OWASP ZAP
# استخدم GUI tool
```

---

## ⚡ الاستجابة للطوارئ

### عند اكتشاف اختراق

**الخطوات الفورية:**

1. **أوقف الخادم فوراً**
   ```bash
   Ctrl + C
   # أو
   pkill -f pentest_dashboard.py
   ```

2. **غيّر جميع كلمات المرور**

3. **راجع جميع السجلات**
   ```bash
   cat logs/*.log
   ```

4. **افحص الاتصالات النشطة**
   ```bash
   netstat -tulpn | grep 5000
   ```

5. **أعد التشغيل بإعدادات أكثر أماناً**

---

## 📝 Checklist الأمان الشامل

### قبل الاستخدام
- [ ] غيّرت كلمة المرور الافتراضية
- [ ] فحصت الشبكة المحلية
- [ ] فعّلت Firewall
- [ ] راجعت الـ IPs المسموح لها
- [ ] أعددت نظام Logging

### أثناء الاستخدام
- [ ] أراقب Logs باستمرار
- [ ] أسجل خروج عند الانتهاء
- [ ] أتحقق من الاتصالات النشطة
- [ ] أراقب استهلاك الموارد

### بعد الاستخدام
- [ ] سجلت خروج
- [ ] أوقفت جميع Listeners
- [ ] راجعت جميع السجلات
- [ ] حذفت البيانات الحساسة
- [ ] أمنت النسخ الاحتياطية

---

## 🎓 موارد إضافية

### للتعلم أكثر عن الأمان

- OWASP Top 10
- Flask Security Best Practices
- Python Security Guidelines
- Penetration Testing Ethics

### أدوات مفيدة

- Burp Suite
- Metasploit
- Wireshark
- tcpdump

---

## ⚠️ إخلاء المسؤولية

هذا النظام مصمم **حصرياً** للاستخدام الأخلاقي والقانوني في:
- بيئات الاختبار المعزولة
- Labs التعليمية
- Penetration Testing المصرح به

**المطور غير مسؤول عن:**
- أي استخدام غير قانوني
- أي ضرر ناتج عن سوء الاستخدام
- اختراق أنظمة بدون إذن

---

## 📞 الإبلاغ عن ثغرات أمنية

إذا اكتشفت ثغرة أمنية:
1. **لا تنشرها علناً**
2. سجل تفاصيلها
3. أرسل تقرير مفصل
4. انتظر التحديث الأمني

---

**تذكر: الأمان مسؤولية الجميع! 🛡️**

*"Security is not a product, but a process"* - Bruce Schneier
