# 🔥 HAMZA SKU C2 Dashboard - REAL VERSION

## لوحة تحكم حقيقية 100% لاختبار الاختراق

### ⚡ النسخة الحقيقية - بدون بيانات وهمية

هذه النسخة **حقيقية 100%** وتعمل مع اتصالات فعلية فقط. لا توجد أي targets وهمية أو بيانات تجريبية.

---

## 🎯 المميزات الحقيقية

### ✅ ما تم تحسينه في هذه النسخة:

1. **🎯 كشف حقيقي للاتصالات**
   - مراقبة مستمرة للاتصالات الواردة
   - استخدام `netstat` لكشف الأجهزة المتصلة
   - لا توجد أي targets مزيفة

2. **🔍 نظام مسح متقدم**
   - زر "SCAN" للفحص اليدوي
   - كشف تلقائي كل 5 ثوانٍ
   - عرض فوري للأجهزة المكتشفة

3. **💻 طرفية حقيقية**
   - تنفيذ أوامر حقيقية على الخادم
   - عرض النتائج الفعلية
   - دعم جميع أوامر Linux

4. **📊 إحصائيات دقيقة**
   - استخدام CPU و Memory الفعلي
   - عدد الأوامر المنفذة
   - عدد الجلسات النشطة

5. **🔐 نظام أمان قوي**
   - تشفير SHA-256 لكلمة المرور
   - حماية جميع الـ endpoints
   - سجل كامل لجميع العمليات

---

## 📋 المتطلبات

### الأساسية:
```bash
Python 3.8+
pip3
netcat (nc)
```

### المكتبات (في requirements.txt):
- Flask 3.0.0
- Flask-SocketIO 5.3.5
- psutil 5.9.6
- python-socketio 5.10.0
- eventlet 0.33.3

---

## 🚀 التثبيت السريع

### الطريقة 1: استخدام السكريبت التلقائي (موصى به)

```bash
chmod +x setup.sh
./setup.sh
```

### الطريقة 2: التثبيت اليدوي

```bash
# 1. تثبيت المكتبات
pip3 install -r requirements.txt --break-system-packages

# 2. التأكد من وجود netcat
sudo apt-get install netcat-traditional netcat-openbsd

# 3. تشغيل Dashboard
python3 pentest_dashboard.py
```

---

## 🎮 طريقة الاستخدام

### الخطوة 1: تشغيل Dashboard

```bash
python3 pentest_dashboard.py
```

ستظهر رسالة:
```
╔═══════════════════════════════════════════════════════════╗
║   🔥 PenTest C2 Dashboard - REAL VERSION                 ║
║   Dashboard حقيقي 100% - لا توجد targets وهمية          ║
╚═══════════════════════════════════════════════════════════╝

📍 Dashboard URL: http://localhost:5000
🔐 Password: hamza_sku_2026
```

### الخطوة 2: تسجيل الدخول

1. افتح المتصفح على: `http://localhost:5000`
2. أدخل كلمة المرور: `hamza_sku_2026`
3. اضغط "ACCESS DASHBOARD"

### الخطوة 3: بدء الـ Listener

1. في قسم "Listener Control":
   - Host: `0.0.0.0` (للاستماع على جميع الواجهات)
   - Port: `4444` (أو أي منفذ تريده)
2. اضغط "🚀 START SERVER"

### الخطوة 4: الاتصال من الجهاز الهدف

من الجهاز/الهاتف الذي تريد اختباره:

```bash
# استبدل YOUR_IP بـ IP الخاص بك
nc YOUR_IP 4444
```

مثال:
```bash
nc 192.168.1.100 4444
```

### الخطوة 5: مشاهدة الأهداف

- سيظهر الجهاز المتصل تلقائياً في جدول "Active Targets"
- إذا لم يظهر، اضغط زر "🔍 SCAN"
- ستظهر alert: "🎯 New Victim Detected!"

---

## 🔍 كيف يعمل الكشف الحقيقي؟

### 1. المراقبة المستمرة (Auto-Detection)
```python
# يراقب كل 5 ثواني:
- يستخدم netstat لعرض الاتصالات النشطة
- يبحث عن connections على المنفذ المحدد
- يستخرج IP Address
- يضيفه للقائمة تلقائياً
```

### 2. الفحص اليدوي (Manual Scan)
```python
# عند الضغط على زر SCAN:
- ينفذ: netstat -tn | grep :PORT | grep ESTABLISHED
- يستخرج جميع الـ IPs المتصلة
- يضيف الجديد فقط للقائمة
```

### 3. كشف نظام التشغيل
```python
# محاولة معرفة نظام التشغيل:
- يستخدم ping لفحص TTL
- TTL=64 → Linux/Android
- TTL=128 → Windows
- TTL=255 → iOS/MacOS
```

---

## 📊 معلومات النظام

### الإحصائيات الحقيقية:
- **CPU Usage**: استخدام المعالج الفعلي (psutil)
- **Memory**: استخدام الذاكرة الفعلي (psutil)
- **Commands**: عدد الأوامر المنفذة
- **Sessions**: عدد الأهداف النشطة
- **Uptime**: وقت التشغيل

### السجلات (Logs):
- تسجيل جميع العمليات
- تصنيف حسب المستوى (SUCCESS, ERROR, WARNING, INFO)
- عرض الوقت والتاريخ
- تحديث فوري عبر WebSocket

---

## 🛠️ استكشاف الأخطاء

### المشكلة: الـ Listener لا يبدأ

**الحل:**
```bash
# تحقق من المنفذ:
sudo lsof -i :4444

# إذا كان مشغول:
sudo lsof -ti:4444 | xargs kill -9

# ثم حاول مرة أخرى
```

### المشكلة: لا تظهر Targets

**الحل:**
```bash
# 1. تأكد من تشغيل الـ Listener
# 2. اضغط زر "🔍 SCAN"
# 3. تحقق من الاتصال الفعلي:
netstat -tn | grep :4444 | grep ESTABLISHED
```

### المشكلة: Firewall يمنع الاتصال

**الحل:**
```bash
# السماح بالمنفذ في Firewall:
sudo ufw allow 4444

# أو إيقاف Firewall مؤقتاً للاختبار:
sudo ufw disable
```

### المشكلة: خطأ في تثبيت المكتبات

**الحل:**
```bash
# استخدم --break-system-packages:
pip3 install -r requirements.txt --break-system-packages

# أو استخدم virtual environment:
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

---

## 🔐 الأمان

### ⚠️ تحذيرات مهمة جداً:

1. **للاستخدام القانوني فقط**
   - استخدم فقط على أجهزتك الخاصة
   - أو مع إذن كتابي من صاحب الجهاز
   - اختبار الاختراق غير المصرح به جريمة

2. **الحماية**
   - استخدم فقط في شبكة محلية معزولة
   - لا تعرض Dashboard على الإنترنت
   - غيّر كلمة المرور في الإنتاج

3. **الخصوصية**
   - احذف السجلات بعد الاستخدام
   - لا تشارك معلومات حساسة
   - استخدم VPN في بيئات الاختبار

---

## 🎯 حالات الاستخدام

### 1. اختبار الشبكة المحلية
```bash
# شغّل Dashboard على جهازك
# اتصل من أجهزة أخرى في الشبكة
# راقب الاتصالات والأداء
```

### 2. التدريب والتعليم
```bash
# استخدمه لتعليم مفاهيم C2
# شرح كيفية عمل Listeners
# توضيح تقنيات اختبار الاختراق
```

### 3. اختبار الحماية
```bash
# اختبار Firewall rules
# فحص IDS/IPS systems
# تقييم استجابة الشبكة
```

---

## 🔧 التخصيص

### تغيير كلمة المرور:

1. افتح `pentest_dashboard.py`
2. ابحث عن:
```python
ADMIN_PASSWORD_HASH = "8f3e8c8f7c5c4e5f9e6a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e"
```
3. استبدله بـ hash جديد:
```python
import hashlib
new_password = "your_new_password"
hash = hashlib.sha256(new_password.encode()).hexdigest()
print(hash)
```

### تغيير المنفذ الافتراضي:

في `dashboard.html`، غيّر:
```javascript
<input type="number" id="portInput" class="control-input" value="4444">
```

### تغيير الألوان:

في `dashboard.html`، عدّل:
```css
:root {
    --primary: #00ff88;  /* غيّر هذا */
    --secondary: #0088ff; /* وهذا */
}
```

---

## 📝 الفرق بين النسخة الحقيقية والقديمة

| الميزة | النسخة القديمة | النسخة الحقيقية |
|--------|----------------|------------------|
| Targets | وهمية | حقيقية فقط ✅ |
| Auto-Detection | ❌ لا | ✅ نعم |
| Manual Scan | ❌ لا | ✅ نعم |
| Real Listener | ❌ لا | ✅ نعم (netcat) |
| OS Detection | ❌ لا | ✅ نعم (TTL-based) |
| Live Updates | ❌ محدود | ✅ WebSocket |

---

## 🤝 المساهمة

نرحب بالمساهمات لتحسين المشروع:

- الإبلاغ عن الأخطاء
- اقتراح ميزات جديدة
- تحسين الكود
- إضافة وثائق

---

## 📞 الدعم

### إذا واجهت مشاكل:

1. **تحقق من الـ Logs**:
   ```bash
   # في Dashboard → System Logs
   # أو في Terminal حيث يعمل Python
   ```

2. **اختبر الاتصال**:
   ```bash
   # من Terminal منفصل:
   nc -zv localhost 4444
   netstat -tn | grep :4444
   ```

3. **راجع الأذونات**:
   ```bash
   chmod +x pentest_dashboard.py
   chmod +x setup.sh
   ```

---

## 📄 الترخيص

هذا المشروع مخصص للأغراض التعليمية واختبار الاختراق الأخلاقي فقط.

**المطور غير مسؤول عن:**
- أي استخدام غير قانوني
- أي أضرار ناتجة عن سوء الاستخدام
- أي انتهاك للخصوصية أو القوانين

---

## 🎓 موارد إضافية

### تعلم المزيد عن:
- [Netcat Usage](https://www.sans.org/reading-room/whitepapers/testing/netcat-swiss-army-knife-pen-testers-33069)
- [Penetration Testing Basics](https://www.offensive-security.com/)
- [Python Socket Programming](https://docs.python.org/3/library/socket.html)
- [Flask Web Development](https://flask.palletsprojects.com/)

---

## ✅ Checklist قبل الاستخدام

- [ ] قرأت وفهمت التحذيرات الأمنية
- [ ] حصلت على إذن كتابي للاختبار
- [ ] في شبكة معزولة/محلية
- [ ] ثبّت جميع المتطلبات
- [ ] غيّرت كلمة المرور الافتراضية (للإنتاج)
- [ ] أعددت Firewall بشكل صحيح
- [ ] جاهز للبدء!

---

**🔥 استمتع باستخدام HAMZA SKU C2 Dashboard - Real Version! 🔥**

*"With great power comes great responsibility"*
*"القوة الكبيرة تأتي مع مسؤولية كبيرة"*

---

## 📸 لقطات الشاشة

### Login Page
صفحة تسجيل دخول احترافية بتصميم Cyberpunk

### Dashboard
- لوحة تحكم كاملة
- إحصائيات حية
- طرفية تفاعلية
- جدول الأهداف
- سجل العمليات

### Features
- ✅ Real-time detection
- ✅ Manual scanning
- ✅ Live statistics
- ✅ Secure authentication
- ✅ Beautiful UI

---

**Made with 🔥 by HAMZA SKU**
**Version: 1.0.0 - Real Edition**
**Date: 2026**
