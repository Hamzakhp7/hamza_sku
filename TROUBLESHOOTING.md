# 🔧 TROUBLESHOOTING GUIDE - Real Version
# دليل حل المشاكل - النسخة الحقيقية

## 🎯 المشكلة: لا تظهر Targets في Dashboard

### ✅ الحل الكامل (خطوة بخطوة):

#### 1. تأكد من تشغيل Dashboard
```bash
# افحص إذا كان يعمل:
ps aux | grep pentest_dashboard.py

# إذا لم يكن يعمل، شغّله:
python3 pentest_dashboard.py
```

#### 2. تأكد من بدء Listener
```bash
# في Dashboard، تحقق من:
# Status = ONLINE (وليس OFFLINE)
# Port = الرقم الصحيح (مثل 4444)

# أو افحص من Terminal:
netstat -tln | grep :4444
# أو
ss -tln | grep :4444
```

#### 3. اتصل من الجهاز الهدف
```bash
# من الهاتف/الجهاز الآخر:
nc YOUR_COMPUTER_IP 4444

# مثال:
nc 192.168.1.100 4444
```

#### 4. استخدم زر SCAN
```bash
# في Dashboard:
# اذهب لقسم "Active Targets"
# اضغط زر "🔍 SCAN"
# انتظر 2-3 ثواني
# يجب أن تظهر جميع الاتصالات النشطة
```

---

## 🔍 أداة التشخيص التلقائية

### استخدام diagnose.sh:
```bash
chmod +x diagnose.sh
./diagnose.sh
```

ستفحص الأداة:
- ✅ حالة Dashboard
- ✅ حالة Listener
- ✅ الاتصالات النشطة
- ✅ Firewall rules
- ✅ Network interfaces
- ✅ Python packages
- ✅ وأكثر...

---

## ⚠️ المشاكل الشائعة والحلول

### مشكلة 1: "Port already in use"

**السبب:** المنفذ مشغول بواسطة عملية أخرى

**الحل:**
```bash
# معرفة العملية المستخدمة للمنفذ:
sudo lsof -i :4444

# إنهاء العملية:
sudo lsof -ti:4444 | xargs kill -9

# أو استخدم منفذ آخر في Dashboard
```

### مشكلة 2: "Connection refused"

**الأسباب المحتملة:**
- Listener غير مفعّل
- Firewall يمنع الاتصال
- IP أو Port خاطئ

**الحلول:**
```bash
# 1. تأكد من Listener:
netstat -tln | grep :4444

# 2. تعطيل Firewall مؤقتاً للاختبار:
sudo ufw disable

# 3. اختبر الاتصال محلياً:
nc -zv localhost 4444

# 4. اختبر من جهاز آخر:
nc -zv YOUR_IP 4444
```

### مشكلة 3: "Permission denied"

**السبب:** منافذ أقل من 1024 تحتاج صلاحيات root

**الحل:**
```bash
# استخدم منفذ أكبر من 1024:
# مثل: 4444, 8080, 9999

# أو شغّل بصلاحيات root:
sudo python3 pentest_dashboard.py
```

### مشكلة 4: Target يختفي بعد ظهوره

**السبب:** الاتصال انقطع

**الحل:**
```bash
# تحقق من استقرار الاتصال:
watch -n 1 'netstat -tn | grep :4444'

# تحقق من Timeout settings في Target
# أعد الاتصال من Target
```

### مشكلة 5: "Module not found"

**السبب:** مكتبات Python غير مثبتة

**الحل:**
```bash
# ثبّت جميع المتطلبات:
pip3 install -r requirements.txt --break-system-packages

# أو واحدة واحدة:
pip3 install Flask Flask-SocketIO psutil eventlet
```

### مشكلة 6: Dashboard بطيء جداً

**السبب:** موارد النظام محدودة

**الحل:**
```bash
# قلل تحديثات الإحصائيات:
# عدّل في pentest_dashboard.py:
# time.sleep(2) → time.sleep(5)

# أغلق برامج غير ضرورية
# استخدم lightweight terminal
```

### مشكلة 7: "Cannot connect to localhost:5000"

**السبب:** Dashboard لا يعمل على المنفذ الصحيح

**الحل:**
```bash
# تحقق من المنفذ:
netstat -tln | grep :5000

# أعد تشغيل Dashboard:
pkill -f pentest_dashboard.py
python3 pentest_dashboard.py

# جرب منفذ آخر:
# عدّل في pentest_dashboard.py آخر سطر:
# port=5000 → port=8080
```

---

## 🔐 مشاكل تسجيل الدخول

### "Invalid password"

**الحل:**
```bash
# تأكد من كلمة المرور:
# hamza_sku_2026 (انتبه للأحرف الكبيرة والصغيرة)

# إذا نسيتها، أعد تعيينها:
# في pentest_dashboard.py:
import hashlib
print(hashlib.sha256("your_new_password".encode()).hexdigest())
# استبدل ADMIN_PASSWORD_HASH بالناتج
```

### "Session expired"

**الحل:**
```bash
# امسح الكوكيز في المتصفح
# أو استخدم وضع التصفح الخاص
# أو أعد تشغيل Dashboard
```

---

## 🌐 مشاكل الشبكة

### لا يمكن الاتصال من الشبكة

**الحل:**
```bash
# 1. تأكد من Host = 0.0.0.0 (وليس 127.0.0.1)

# 2. معرفة IP الخاص بك:
ip addr show | grep "inet "
# أو
ifconfig | grep "inet "

# 3. اختبر الاتصال:
# من جهاز آخر في الشبكة:
ping YOUR_IP
nc -zv YOUR_IP 4444

# 4. تحقق من Router/NAT settings
```

### الاتصال بطيء جداً

**الحل:**
```bash
# قلل البيانات المرسلة:
# استخدم compression
# قلل تحديثات WebSocket

# تحقق من جودة الشبكة:
ping YOUR_IP
```

---

## 📊 مشاكل الإحصائيات

### الإحصائيات لا تتحدث

**الحل:**
```bash
# تحقق من WebSocket:
# افتح Developer Tools في المتصفح
# Console → تحقق من أخطاء socket.io

# أعد تحميل الصفحة:
Ctrl+Shift+R (Hard Reload)

# تحقق من اتصال Socket.IO:
# يجب أن ترى: "Connected to server" في Console
```

---

## 🎯 نصائح للحصول على أفضل النتائج

### 1. استخدم المنافذ الصحيحة
```bash
# موصى به:
# 4444 - المنفذ الافتراضي لـ Metasploit
# 8080 - بديل شائع
# 9999 - آخر شائع

# تجنب:
# 443 - يحتاج root
# 80 - يحتاج root
# 22 - SSH port
```

### 2. في بيئة الاختبار
```bash
# عطّل Firewall مؤقتاً:
sudo ufw disable

# أو اسمح بالمنفذ:
sudo ufw allow 4444

# بعد الانتهاء، أعد تفعيله:
sudo ufw enable
```

### 3. راقب السجلات
```bash
# في Dashboard:
# راقب قسم "System Logs" بشكل مستمر

# في Terminal:
# لاحظ أي أخطاء أو warnings

# استخدم:
tail -f /var/log/syslog | grep python
```

### 4. اختبر خطوة خطوة
```bash
# 1. شغّل Dashboard
# 2. سجّل دخول
# 3. ابدأ Listener
# 4. اختبر محلياً أولاً:
nc localhost 4444

# 5. ثم اختبر من الشبكة:
nc YOUR_IP 4444

# 6. استخدم SCAN إذا لزم الأمر
```

---

## 🔄 إعادة التعيين الكاملة

### إذا فشل كل شيء، أعد البدء من الصفر:

```bash
# 1. أوقف كل شيء:
pkill -f pentest_dashboard.py
sudo lsof -ti:4444 | xargs kill -9
sudo lsof -ti:5000 | xargs kill -9

# 2. امسح الكوكيز والكاش:
# في المتصفح

# 3. أعد تثبيت المتطلبات:
pip3 uninstall -y Flask Flask-SocketIO psutil eventlet
pip3 install -r requirements.txt --break-system-packages

# 4. أعد تشغيل Dashboard:
python3 pentest_dashboard.py

# 5. ابدأ من الخطوة 1
```

---

## 📝 سجل الاختبار (Checklist)

### قبل أن تقول "لا يعمل"، تحقق من:

- [ ] Dashboard يعمل (ps aux | grep pentest)
- [ ] تسجيل دخول ناجح (كلمة مرور صحيحة)
- [ ] Listener مفعّل (Status = ONLINE)
- [ ] Port صحيح (4444 أو ما اخترته)
- [ ] Host = 0.0.0.0
- [ ] Firewall معطّل أو السماح بالمنفذ
- [ ] IP صحيح (ليس 127.0.0.1 للشبكة)
- [ ] Connection من Target فعلي (nc YOUR_IP PORT)
- [ ] زر SCAN تم الضغط عليه
- [ ] Logs لا تظهر أخطاء

---

## 💡 فهم الأخطاء الشائعة

### رسالة الخطأ: "Address already in use"
```
المعنى: المنفذ مشغول
الحل: غيّر المنفذ أو أنهِ العملية الأخرى
```

### رسالة الخطأ: "Connection refused"
```
المعنى: لا يوجد شيء يستمع على المنفذ
الحل: ابدأ Listener في Dashboard
```

### رسالة الخطأ: "Permission denied"
```
المعنى: تحتاج صلاحيات أعلى
الحل: استخدم منفذ > 1024 أو sudo
```

### رسالة الخطأ: "Network unreachable"
```
المعنى: مشكلة في الشبكة
الحل: تحقق من الاتصال والـ IP
```

---

## 🆘 الحصول على المساعدة

### 1. جمع المعلومات:
```bash
# شغّل أداة التشخيص:
./diagnose.sh > diagnosis.txt

# جمع الـ logs:
tail -n 100 /var/log/syslog | grep python > logs.txt
```

### 2. حاول الحلول الأساسية أولاً:
- أعد تشغيل Dashboard
- أعد تشغيل الكمبيوتر
- استخدم متصفح آخر
- جرب شبكة أخرى

### 3. ابحث في الوثائق:
- README.md
- TROUBLESHOOTING.md (هذا الملف)
- تعليقات الكود

---

## ✅ الخلاصة

### القواعد الذهبية:

1. **استخدم منفذ > 1024** (مثل 4444)
2. **Host = 0.0.0.0** للشبكة
3. **اضغط SCAN** بعد الاتصال
4. **راقب الـ Logs** دائماً
5. **اختبر محلياً أولاً** ثم الشبكة
6. **استخدم diagnose.sh** عند المشاكل

---

**إذا اتبعت هذا الدليل، 99% من المشاكل ستُحل! 🎯**

**Need more help? Check the code comments or README.md**

---

**Made with 🔥 by HAMZA SKU**
**Version: 1.0.0 - Real Edition**
